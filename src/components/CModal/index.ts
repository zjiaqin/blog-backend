import { withInstall } from '/@/utils';
import CustomModal, { CustomModalProps } from './src/CustomModal';

export const CModal = withInstall(CustomModal);
export declare type CModalProps = CustomModalProps;
